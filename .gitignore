node_modules/
.next/
next-env.d.ts
*.tsbuildinfo

.env
.env.local
.env*.local

# Written by `vercel link`: the project and team ids, and nothing secret. The
# CLI adds `.env*` beside it, which would swallow the tracked `.env.example`.
.vercel
